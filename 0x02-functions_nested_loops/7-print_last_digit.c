#include "main.h"

/**
 * print_last_digit - entry point
 * @a: parameter to check
 * Return: returns last digit
 */
int print_last_digit(int a)
{
	int last_n;

	last_n = a % 10;

	return (last_n);
}
