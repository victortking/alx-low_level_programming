#include "main.h"
#include <string.h>

/**
 * _strlen - function for string param
 * @s: param to check for length
 * Return: returns string length
 */

int _strlen(char *s)
{
	int l = strlen(s);

	return (l);
}
