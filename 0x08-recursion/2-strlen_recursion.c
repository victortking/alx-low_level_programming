#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: param to check for length
 * Return: returns length
 */
int _strlen_recursion(char *s)
{
	int num = strlen(s);

	return (num);
}
